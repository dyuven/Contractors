# Uncomment the next line to define a global platform for your project
# platform :ios, '13.0'

target 'TestWork' do
   pod 'RealmSwift', '~> 5.5.1', :modular_headers => true
pod 'Realm', '~> 5.5.1', :modular_headers => true
end
